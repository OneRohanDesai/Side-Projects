
apiVersion: v1
kind: Service
metadata:
  name: kiribati-metrics
  labels:
    app: country-sim
    country: kiribati

spec:
  selector:
    app: country-sim
    country: kiribati

  ports:
  - name: metrics
    port: 8080
    targetPort: 8080
