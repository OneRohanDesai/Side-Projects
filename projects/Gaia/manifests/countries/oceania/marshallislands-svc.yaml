
apiVersion: v1
kind: Service
metadata:
  name: marshallislands-metrics
  labels:
    app: country-sim
    country: marshallislands

spec:
  selector:
    app: country-sim
    country: marshallislands

  ports:
  - name: metrics
    port: 8080
    targetPort: 8080
