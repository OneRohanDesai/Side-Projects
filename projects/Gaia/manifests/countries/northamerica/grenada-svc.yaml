
apiVersion: v1
kind: Service
metadata:
  name: grenada-metrics
  labels:
    app: country-sim
    country: grenada

spec:
  selector:
    app: country-sim
    country: grenada

  ports:
  - name: metrics
    port: 8080
    targetPort: 8080
