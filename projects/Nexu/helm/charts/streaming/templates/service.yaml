apiVersion: v1
kind: Service
metadata:
  name: {{ include "streaming.fullname" . }}
  labels:
    app: {{ include "streaming.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "streaming.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}

