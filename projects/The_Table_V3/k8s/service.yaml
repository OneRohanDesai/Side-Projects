apiVersion: v1
kind: Service
metadata:
  name: the-table
  namespace: thetable
  labels:
    app: the-table
spec:
  type: NodePort
  selector:
    app: the-table
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
