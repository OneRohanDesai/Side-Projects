apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-provider
  namespace: {{ .Values.namespace }}
  labels:
    app: grafana
data:
  dashboards.yaml: |
{{ .Files.Get "files/grafana/providers/dashboard-provider.yaml" | nindent 4 }}
