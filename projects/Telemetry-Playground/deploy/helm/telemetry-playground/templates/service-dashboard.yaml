apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: {{ include "telemetry.namespace" . }}
  labels:
    {{- include "telemetry.labels" . | nindent 4 }}
    app: dashboard
spec:
  type: {{ .Values.service.type }}
  selector:
    app: dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
