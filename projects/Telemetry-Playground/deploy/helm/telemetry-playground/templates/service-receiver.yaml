apiVersion: v1
kind: Service
metadata:
  name: receiver
  namespace: {{ include "telemetry.namespace" . }}
  labels:
    {{- include "telemetry.labels" . | nindent 4 }}
    app: receiver
spec:
  type: {{ .Values.service.type }}
  selector:
    app: receiver
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
