apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: generator
  namespace: {{ include "telemetry.namespace" . }}
  labels:
    {{- include "telemetry.labels" . | nindent 4 }}
    app: generator
spec:
  serviceName: generator
  replicas: {{ .Values.replicas.generator }}
  podManagementPolicy: Parallel
  selector:
    matchLabels:
      app: generator
  template:
    metadata:
      labels:
        app: generator
        {{- include "telemetry.selectorLabels" . | nindent 8 }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: generator
          image: {{ include "telemetry.image" (list .Values.image.generator .Values.imageTag) }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          env:
            - name: GENERATOR_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: REDIS_HOST
              value: {{ .Values.config.redisHost | quote }}
            - name: REDIS_PORT
              value: {{ .Values.config.redisPort | quote }}
            - name: RECEIVER_URL
              value: {{ .Values.config.receiverUrl | quote }}
            - name: OTEL_SERVICE_NAME
              value: generator
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: service.namespace=telemetry
            {{- include "telemetry.otelEnv" . | nindent 12 }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
          resources:
            requests:
              cpu: {{ .Values.resources.generator.requests.cpu }}
              memory: {{ .Values.resources.generator.requests.memory }}
            limits:
              cpu: {{ .Values.resources.generator.limits.cpu }}
              memory: {{ .Values.resources.generator.limits.memory }}
