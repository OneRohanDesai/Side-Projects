apiVersion: v1
kind: Secret
metadata:
  name: telemetry-secret
  namespace: {{ include "telemetry.namespace" . }}
  labels:
    {{- include "telemetry.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Placeholder for future shared secrets (Vault / SealedSecrets later).
  PLACEHOLDER: "unused"
