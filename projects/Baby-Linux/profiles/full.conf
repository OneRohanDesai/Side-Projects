# Everything — daily driver (desktop + platform stack)
# Tool depth still prompted per module unless -y (then advanced)
PROFILE_MODULES="base shell desktop devtools devops sre cloud security virt laptop post"
