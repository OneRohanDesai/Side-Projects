/**
 * Vault API base URL (Cloudflare Worker).
 * After `npm run deploy` in this folder, paste your workers.dev URL here.
 * Leave empty only when page and API share the same origin.
 */
window.VAULT_API = "https://vault-api.rohandesai98244.workers.dev";
